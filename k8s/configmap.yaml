apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-inference-config
  namespace: ai-workload
data:
  LOG_LEVEL: "INFO"
  WORKERS: "4"
  MODEL_VERSION: "1.0.0"
  MAX_BATCH_SIZE: "32"
  TIMEOUT_SECONDS: "30"
