apiVersion: v1
kind: Service
metadata:
  name: ai-inference-service
  namespace: ai-workload
  labels:
    app: ai-inference
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-inference
